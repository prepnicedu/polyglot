# ./deploy/chart/BUILD.bazel

load("@rules_helm//helm:defs.bzl", "helm_chart")

package(default_visibility = ["//visibility:public"])

helm_chart(
    name = "microservice_chart",
    chart = "Chart.yaml",
    templates = glob(["templates/**/*.yaml"]),
    values = "values.yaml",
)
